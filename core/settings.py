"""Settings for server."""
# Uncomment the settings to enable your change or the default values will be used.

# Cache
# Implement cache engine in `cache.py`. Then add the `class` name of cache engine here.
cache_engines = {
    'redis': 'RedisCache',
}

# Redis

# redis_host = 'localhost'
# redis_port = 6379
# redis_db = 0
